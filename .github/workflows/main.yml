# Create a folder
$ mkdir actions-runner && cd actions-runner# Download the latest runner package
$ curl -o actions-runner-linux-arm64-2.305.0.tar.gz -L https://github.com/actions/runner/releases/download/v2.305.0/actions-runner-linux-arm64-2.305.0.tar.gz# Optional: Validate the hash
$ echo "63d7b0ba495055e390ac057dc67d721ed78113990fa837a20b141a75044e152a  actions-runner-linux-arm64-2.305.0.tar.gz" | shasum -a 256 -c# Extract the installer
$ tar xzf ./actions-runner-linux-arm64-2.305.0.tar.gz

# Create the runner and start the configuration experience
$ ./config.sh --url https://github.com/atamshkai/termux-app --token APZ5KAI25F4YDGVP6VENO63ESEKPQ# Last step, run it!
$ ./run.sh

# Use this YAML in your workflow file for each job
runs-on: self-hosted
